# Python Dependencies for Economic Data Analysis Pipeline
# Install with: pip install -r requirements.txt

# Core data manipulation and analysis
pandas>=2.0.0
numpy>=1.24.0

# API requests and data fetching
requests>=2.31.0

# Web scraping for public opinion data
beautifulsoup4>=4.12.0

# YAML configuration file parsing
pyyaml>=6.0

# Statistical analysis (Extension 1: Statistical Analyzer)
scipy>=1.10.0
statsmodels>=0.14.0

# Optional: For enhanced data visualization in Python (if extending the project)
matplotlib>=3.7.0
seaborn>=0.12.0

# Note: External dependencies (must be installed separately)
#
# R packages - run in R console:
#   install.packages(c("ggplot2", "dplyr", "readr", "scales", "patchwork"))
#
# Quarto - for report generation (Extension 2: Report Generator)
#   macOS: brew install quarto
#   Other: Download from https://quarto.org/docs/get-started/
